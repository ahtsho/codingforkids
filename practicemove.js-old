class PracticeMove extends Game {
	constructor(){
		super();
		this.robot = new Robot(super.getRandNum(0,5),super.getRandNum(0,5), 0);
		this.robot.draw();
		this.pila = new Battery(super.getRandNum(0,5),super.getRandNum(0,5));
		this.pila.draw();
	}
}